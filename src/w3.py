from config import parse_args, the
from data import DATA
import eg

# weather = DATA("../data/weather.csv") TODO: Implement Task 1 Solution

# classCount = 0
# rowCount = len(weather.rows)
# klass = weather.cols.klass

# print(klass.has)

# # for col in weather.cols.x: print(weather.cols.x[col].txt)

# for col in weather.cols.x:
#     classCount += 1

# print("Class Count: ", classCount)
# print("Row Count: ", rowCount)

diabetes = DATA("../data/diabetes.csv")
soybean = DATA("../data/soybean.csv")


